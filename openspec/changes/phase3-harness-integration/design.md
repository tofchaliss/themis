# Design — phase3-harness-integration

Source of truth: `docs/engineering/decisions/EDR-HARNESS-01.md` (this file is the realization map).

| EDR decision | Realization |
|---|---|
| D1 Commission record | `domain.Commission`, `Finding.Commission`, `ReconstituteCommission(s)`; `finding_commissions` (000014) |
| D2 Lifecycle | `Finding.WithdrawCommission` (forward-only, any human); `FindingService.WithdrawCommission` |
| D3 No stage effect; thin events | `Commission` never touches `stage`; `FindingCommissioned`/`CommissionWithdrawn` mapped to `governance.finding_commissioned.v1` / `governance.commission_withdrawn.v1` |
| D4 Human only | `NewCommission` refuses non-human actors; HTTP actor derived by `actorProvenance` (D10) |
| D5 Evidence + derived trust | `domain.HarnessExecution` (+`Validate`, `DerivedTrust`), `NewHarnessProposal`; `finding_proposals.harness_evidence JSONB` + `commission_id`; `HarnessExecutionEvidence` schema; `RaiseProposalRequest.evidence` + `evidence_trust` |
| D6 Correspondence | `HarnessExecution.Corresponds`; `FindingService.RaiseHarnessProposal` (order: shape → trust → commission on this Finding → correspondence → Business Verification → raise) |
| D7 Decision door | unchanged `AcceptProposal`; Position cites the proposal |
| D8 Placement/walls | `internal/governance/adapters/harness` (Resolve, Evidence, witnessing table); `cmd/themis-intake`; `.golangci.yml` `governance-harness-adapter-readonly`; `tests/architecture/harness_test.go` |
| D9 Delegations | `Resolution.Delegations` rendered in the evidence view; `HarnessDelegations{Count, Seqs}` in the evidence; `premiseL8ToolLess` on every table entry |

Fixture provenance (D-I-7): `internal/governance/adapters/harness/testdata/fixtures/<constitution12>/`
is a REAL runtime record generated by the runtime repository's `TestThemisIntakeFixture` and
copied here with its `provenance.json`; the adapter tests fail loudly when the compiled runtime
constitution differs from the fixture's.
