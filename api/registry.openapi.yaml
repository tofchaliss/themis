openapi: 3.0.3
info:
  title: Themis Registry API
  version: 1.0.0
  description: >
    Registry supporting context (Phase-3 greenfield) — register the
    Product → Project → Release structural identity hierarchy and look up Releases.
    Identity + structure only; no security state (Governance owns that).
servers:
  - url: /api/v1
paths:
  /products:
    post:
      operationId: registerProduct
      summary: Register a product; returns a stable Product ID.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterProductRequest'
      responses:
        '201':
          description: Registered.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegisterResponse'
        '400':
          $ref: '#/components/responses/Problem'
  /projects:
    post:
      operationId: registerProject
      summary: Register a project under a product; returns a stable Project ID.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterProjectRequest'
      responses:
        '201':
          description: Registered.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegisterResponse'
        '400':
          $ref: '#/components/responses/Problem'
        '422':
          $ref: '#/components/responses/Problem'
  /releases:
    post:
      operationId: registerRelease
      summary: Register a release under a project; returns a stable Release ID.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterReleaseRequest'
      responses:
        '201':
          description: Registered.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegisterResponse'
        '400':
          $ref: '#/components/responses/Problem'
        '422':
          $ref: '#/components/responses/Problem'
    get:
      operationId: listReleases
      summary: List the releases of a project.
      parameters:
        - name: project
          in: query
          required: true
          schema:
            type: string
      responses:
        '200':
          description: OK.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Release'
        '400':
          $ref: '#/components/responses/Problem'
  /releases/{id}:
    get:
      operationId: getRelease
      summary: Get a release by ID.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: OK.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Release'
        '404':
          $ref: '#/components/responses/Problem'
  /customers:
    post:
      operationId: registerCustomer
      summary: Register a customer; returns a stable Customer ID.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterCustomerRequest'
      responses:
        '201':
          description: Registered.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegisterResponse'
        '400':
          $ref: '#/components/responses/Problem'
  /products/{id}/microservices:
    post:
      operationId: registerMicroservice
      summary: Register a microservice under a product; returns a stable Microservice ID.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterMicroserviceRequest'
      responses:
        '201':
          description: Registered.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegisterResponse'
        '400':
          $ref: '#/components/responses/Problem'
        '422':
          $ref: '#/components/responses/Problem'
  /microservices/{id}/deployments:
    post:
      operationId: registerDeployment
      summary: Register a deployment of a microservice for a customer; returns a stable Deployment ID.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterDeploymentRequest'
      responses:
        '201':
          description: Registered.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegisterResponse'
        '400':
          $ref: '#/components/responses/Problem'
        '422':
          $ref: '#/components/responses/Problem'
  /releases/{id}/blast-radius:
    get:
      operationId: getBlastRadius
      summary: Count the unique customers a release reaches through the estate graph.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: OK.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BlastRadius'
        '400':
          $ref: '#/components/responses/Problem'
components:
  responses:
    Problem:
      description: Error.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Problem'
  schemas:
    RegisterProductRequest:
      type: object
      required: [name]
      properties:
        name:
          type: string
    RegisterProjectRequest:
      type: object
      required: [product_id, name]
      properties:
        product_id:
          type: string
        name:
          type: string
    RegisterReleaseRequest:
      type: object
      required: [project_id, version]
      properties:
        project_id:
          type: string
        version:
          type: string
    RegisterResponse:
      type: object
      required: [id]
      properties:
        id:
          type: string
    Release:
      type: object
      properties:
        id:
          type: string
        project_id:
          type: string
        version:
          type: string
    RegisterCustomerRequest:
      type: object
      required: [name]
      properties:
        name:
          type: string
    RegisterMicroserviceRequest:
      type: object
      required: [name]
      properties:
        name:
          type: string
    RegisterDeploymentRequest:
      type: object
      required: [customer_id, environment]
      properties:
        customer_id:
          type: string
        environment:
          type: string
    BlastRadius:
      type: object
      required: [release_id, unique_customers]
      properties:
        release_id:
          type: string
        unique_customers:
          type: integer
    Problem:
      type: object
      properties:
        title:
          type: string
        detail:
          type: string
